/*Create a file called utils.js importing the above data and write a simple function inside the file to just iterate the array and logging each object’s "title" and "body". */

// console.log(module);

var functionsModule = require("./data");

console.log(functionsModule.functionone());

var final = function() {
  //seconds
};

module.exports = {
  functionTwo: final
};
